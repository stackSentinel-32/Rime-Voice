# Full app (live deployment). The voice pipeline runs in the backend process:
# Deepgram + Rime streaming WebSockets, Groq tool decisions — NO LiveKit.
# Pin exact versions at submission time after a clean install.

aiohttp>=3.9
openai>=1.40
redis>=5.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
