# Full app (live deployment) — everything in requirements-dev.txt plus transport/infra.
# The hermetic test suite (./evidence/run_acceptance_test.sh) needs only requirements-dev.txt.
#
# Pin exact versions at submission time after a clean install.

livekit-agents[openai]~=1.2
livekit-plugins-deepgram~=1.2
livekit-plugins-rime~=0.1
livekit-plugins-silero~=1.2
livekit-plugins-openai~=1.2
openai>=1.40
redis>=5.0
sqlalchemy>=2.0
psycopg2-binary>=2.9
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
